-- TODOs
-- title
-- completed

create table todos (
    id serial primary key,
    name text,
    completed boolean
);

-- USERS
-- name

create table users (
    id serial primary key,
    name text
)
